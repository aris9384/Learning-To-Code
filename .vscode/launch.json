{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "file": "${workspaceRoot}/web/simple/index.html",
            "webRoot": "${workspaceRoot}"
        }
    ]
}